CREATE TABLE animais (
    name varchar(20),
    especie varchar(20),
    endereco varchar(20),
    descricao varchar(20),
    filhote boolean,
    status varchar(20),
    id int PRIMARY KEY,
    img varchar(20)
);